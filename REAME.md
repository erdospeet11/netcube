Small test project to try out 3D rendering with Silk.NET.

# Requirements
- .NET 8.0
- Silk.NET
- Silk.NET.OpenGL
- Silk.NET.Windowing

# Running the project
1. Clone the repository
2. Run `dotnet run` in the root of the project